# DEV ONLY: Trust all connections (no password required)
# This file is mounted into the postgres container to prevent
# password desync issues between POSTGRES_PASSWORD env var
# and the actual password stored in the pgdata volume.
local   all   all                          trust
host    all   all   0.0.0.0/0              trust
host    all   all   ::/0                   trust
